name: Arogya AI Enterprise QA CI/CD

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write
  actions: read

jobs:
  selenium-tests:
    name: Selenium - Arogya AI Web Tests (300)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Stage 1 - Checkout Repository
        uses: actions/checkout@v4

      - name: Stage 2 - Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Stage 3 - Install Automation Dependencies
        run: |
          cd arogya_ai_flutter/automation
          npm config set legacy-peer-deps true
          npm install --legacy-peer-deps

      - name: Stage 4 - Execute 300 Selenium Arogya AI Web Tests
        run: |
          cd arogya_ai_flutter/automation
          node tests/runModuleTest.js website

      - name: Stage 5 - Upload Standalone Excel CSV Sheet Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: selenium-arogyaai-web-csv-report-${{ github.run_number }}
          path: arogya_ai_flutter/automation/reports/CSV/
          retention-days: 30

  appium-tests:
    name: Appium - Arogya AI Android App Tests (300)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Stage 1 - Checkout Repository
        uses: actions/checkout@v4

      - name: Stage 2 - Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Stage 3 - Install Appium & Dependencies
        run: |
          cd arogya_ai_flutter/automation
          npm config set legacy-peer-deps true
          npm install --legacy-peer-deps

      - name: Stage 4 - Execute 300 Appium Arogya AI Android App Tests
        run: |
          cd arogya_ai_flutter/automation
          node tests/runModuleTest.js android

      - name: Stage 5 - Upload Standalone Excel CSV Sheet Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: appium-arogyaai-android-csv-report-${{ github.run_number }}
          path: arogya_ai_flutter/automation/reports/CSV/
          retention-days: 30

  unit-tests-api:
    name: Unit Tests - Arogya AI API & Cloud (300)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Stage 1 - Checkout Repository
        uses: actions/checkout@v4

      - name: Stage 2 - Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Stage 3 - Install Backend Dependencies
        run: |
          cd arogya_ai_backend
          npm ci || npm install

      - name: Stage 4 - Execute 300 Unit Tests Arogya AI API & Cloud
        run: |
          cd arogya_ai_flutter/automation
          npm config set legacy-peer-deps true
          npm install --legacy-peer-deps
          node tests/runModuleTest.js api

      - name: Stage 5 - Upload Standalone Excel CSV Sheet Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: unit-tests-arogyaai-api-csv-report-${{ github.run_number }}
          path: arogya_ai_flutter/automation/reports/CSV/
          retention-days: 30

  validation-tests:
    name: Validation Tests - Arogya AI Health Metrics (300)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Stage 1 - Checkout Repository
        uses: actions/checkout@v4

      - name: Stage 2 - Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Stage 3 - Install Dependencies
        run: |
          cd arogya_ai_flutter/automation
          npm config set legacy-peer-deps true
          npm install --legacy-peer-deps

      - name: Stage 4 - Execute 300 Validation Tests Arogya AI Health Metrics
        run: |
          cd arogya_ai_flutter/automation
          node tests/runModuleTest.js validation

      - name: Stage 5 - Upload Standalone Excel CSV Sheet Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: validation-tests-arogyaai-metrics-csv-report-${{ github.run_number }}
          path: arogya_ai_flutter/automation/reports/CSV/
          retention-days: 30

  deployment-status:
    name: Deployment Status - Arogya AI Web & Mobile (300)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Stage 1 - Checkout Repository
        uses: actions/checkout@v4

      - name: Stage 2 - Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Stage 3 - Install Dependencies
        run: |
          cd arogya_ai_flutter/automation
          npm config set legacy-peer-deps true
          npm install --legacy-peer-deps

      - name: Stage 4 - Execute 300 Deployment Status Arogya AI Web & Mobile
        run: |
          cd arogya_ai_flutter/automation
          node tests/runModuleTest.js deployment

      - name: Stage 5 - Upload Standalone Excel CSV Sheet Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: deployment-status-arogyaai-csv-report-${{ github.run_number }}
          path: arogya_ai_flutter/automation/reports/CSV/
          retention-days: 30

  load-tests:
    name: Load Testing - Arogya AI Realtime Sync (300)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Stage 1 - Checkout Repository
        uses: actions/checkout@v4

      - name: Stage 2 - Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Stage 3 - Install Dependencies
        run: |
          cd arogya_ai_flutter/automation
          npm config set legacy-peer-deps true
          npm install --legacy-peer-deps

      - name: Stage 4 - Execute 300 Load Testing Arogya AI Realtime Sync
        run: |
          cd arogya_ai_flutter/automation
          node tests/runModuleTest.js load

      - name: Stage 5 - Upload Standalone Excel CSV Sheet Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: load-testing-arogyaai-realtime-csv-report-${{ github.run_number }}
          path: arogya_ai_flutter/automation/reports/CSV/
          retention-days: 30

  compile-master-report-deploy:
    name: Compile Arogya AI Master Report & Deploy
    runs-on: ubuntu-latest
    needs: [selenium-tests, appium-tests, unit-tests-api, validation-tests, deployment-status, load-tests]
    if: always()

    steps:
      - name: Stage 1 - Checkout Repository
        uses: actions/checkout@v4

      - name: Stage 2 - Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Stage 3 - Install Dependencies
        run: |
          cd arogya_ai_flutter/automation
          npm config set legacy-peer-deps true
          npm install --legacy-peer-deps

      - name: Stage 4 - Download All Standalone Excel CSV Artifacts
        uses: actions/download-artifact@v4
        with:
          path: arogya_ai_flutter/automation/reports/CSV/
          pattern: "*-csv-report-*"
          merge-multiple: true

      - name: Stage 5 - Compile 1,800 Test Cases Arogya AI Master Report
        run: |
          cd arogya_ai_flutter/automation
          node tests/runModuleTest.js compile

      - name: Stage 6 - Upload Arogya AI Master Compiled Workbook Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: arogyaai-master-1800-workbook-${{ github.run_number }}
          path: arogya_ai_flutter/automation/reports/Excel/
          retention-days: 30

      - name: Stage 7 - Deploy Arogya AI Master Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        if: always()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./arogya_ai_flutter/automation/reports/HTML/
          destination_dir: reports/arogya/
          keep_files: true
